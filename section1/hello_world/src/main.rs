// create with cargo "cargo new project_name" and compile/run with "cargo run"
// "cargo build" for build ("cargo build --release" for production builds, generate a optimized executable)

fn main() {
    println!("Hello, World!");
}
